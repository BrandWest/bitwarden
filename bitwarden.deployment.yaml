apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitwarden-pod
  namespace: bitwarden
  labels:
    app: bitwarden
spec:
  selector:
    matchLabels:
      app: bitwarden
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: bitwarden
    spec:
      securityContext:
        fsGroup: 3000
        runAsUser: 3000
        runAsGroup: 3000
      containers:
      - name: bitwarden-pod
        image: vaultwarden/server:latest
        imagePullPolicy: IfNotPresent
        env: 
        - name: SMTP_HOST
          value: <path:secret/data/apps/prod/bitwarden#SMTP_HOST>
        - name: SMTP_PORT
          value: <path:secret/data/apps/prod/bitwarden#SMTP_PORT>
        - name: SMTP_FROM
          value: <path:secret/data/apps/prod/bitwarden#SMTP_FROM>
        - name: SMTP_SECURITY
          value: <path:secret/data/apps/prod/bitwarden#SMTP_SECURITY>
        - name: SMTP_USERNAME
          value: <path:secret/data/apps/prod/bitwarden#SMTP_USERNAME>
        - name: SMTP_PASSWORD
          value: <path:secret/data/apps/prod/bitwarden#SMTP_PASSWORD>
        - name: SIGNUPS_ALLOWED
          value: <path:secret/data/apps/prod/bitwarden#SIGNUPS_ALLOWED>
        - name: INVITATIONS_ALLOWED
          value: <path:secret/data/apps/prod/bitwarden#INVITATIONS_ALLOWED> 
        - name: SSO_ENABLED
          value: <path:secret/data/apps/prod/bitwarden#SSO_ENABLED> 
        - name: SSO_SIGNUPS_MATCH_EMAIL
          value: <path:secret/data/apps/prod/bitwarden#SSO_SIGNUPS_MATCH_EMAIL> 
        - name: SSO_AUTHORITY
          value: <path:secret/data/apps/prod/bitwarden#SSO_AUTHORITY>
        - name: SSO_CLIENT_ID
          value: <path:secret/data/apps/prod/bitwarden#SSO_CLIENT_ID>
        - name: SSO_CLIENT_SECRET 
          value: <path:secret/data/apps/prod/bitwarden#SSO_CLIENT_SECRET>
        - name: DOMAIN
          value: https://<path:secret/data/apps/prod/bitwarden#DOMAIN>
        - name: SSO_CLIENT_CACHE_EXPIRATION
          value: <path:secret/data/apps/prod/bitwarden#SSO_CLIENT_CACHE_EXPIRATION>
        - name: PUSH_ENABLED
          value: <path:secret/data/apps/prod/bitwarden#PUSH_ENABLED>
        - name: PUSH_INSTALLATION_ID
          value: <path:secret/data/apps/prod/bitwarden#PUSH_INSTALLATION_ID>
        - name: PUSH_INSTALLATION_KEY
          value: <path:secret/data/apps/prod/bitwarden#PUSH_INSTALLATION_KEY>
        - name: ROCKET_PORT
          value: "8080"
        envFrom:
        - secretRef:
            name: admin-tokens
        ports:
        - containerPort: 8080
          name: vw-http
          protocol: TCP
        volumeMounts:
        - mountPath: "/data"
          name: bitwarden-storage
          subPath: bitwarden
        resources: {}
      volumes:
      - name: bitwarden-storage
        persistentVolumeClaim:
          claimName: bitwarden-storage-pvc    
