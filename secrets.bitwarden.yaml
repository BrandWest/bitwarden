apiVersion: v1
kind: Secret
metadata:
  name: admin-tokens
  namespace: bitwarden
type: Opaque
stringData:
  ADMIN_TOKEN: <path:secret/data/apps/bitwarden#ADMIN_TOKEN>
  SMTP_PASSWORD: <path:secret/data/apps/bitwarden#SMTP_PASSWORD>
  SMTP_USERNAME: <path:secret/data/apps/bitwarden#SMTP_USERNAME>
  DATABASE_MAX_CONNS: "10"
  ROCKET_LIMITS: "{json=100000000}"
  ROCKET_WORKERS: "10"
